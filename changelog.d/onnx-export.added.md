Add ``anytensor.export`` helpers to serialize AnyTensor callables to ONNX with dynamic / symbolic lengths. Flax/JAX weights rebind as embedded initializers: Torch ``nn.Parameter`` (best names) or in-trace TF constants via ``as_tensorflow_fn`` — not outer tensors or extra graph inputs. Torch segment ops no longer ``int()`` ``num_segments``, so ``at.shape(x)[0]`` stays a symbolic size.
