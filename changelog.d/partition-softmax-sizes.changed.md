Require ``num_segments`` and ``sum_partitions`` on ``partition_softmax`` (JAX shape-size rule; ``num_segments`` matches ``segment_softmax``). Convert with ``partition_ids``; opt-in ``partition_cache()`` reuses ids for the same tensors in a block (GraphNetwork enters one per apply). ``anytensor.jraph.partition_softmax`` keeps official jraph's third positional ``sum_partitions`` but now requires it.
