Require ``num_segments`` and ``sum_partitions`` on ``partition_softmax`` (JAX shape-size rule; ``num_segments`` matches ``segment_softmax``). Ids are rebuilt each call — call ``partition_ids`` once and reuse with ``segment_*``. ``anytensor.jraph.partition_softmax`` keeps official jraph's third positional ``sum_partitions`` but now requires it.
