# Optional imports

Peek at JAX / Torch / TensorFlow (and any other module) **without importing
them**. Used so extras stay unloaded until the caller imports them.

::: anytensor.optional
    options:
      members_order: source
      filters:
        - "!^_"
