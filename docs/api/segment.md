# Segment ops

Required sizes, partition totals, and `cache`: [Usage → Caller rules](../usage.md#caller-rules).

::: anytensor.segment
    options:
      members_order: source
      filters:
        - "!^_"
